const CANNOT_MOVE_DURING_STORM = "Cannot move during a storm"
const CANNOT_MOVE_OUT_OF_BOUND = "Can move only within mapped area"
const OPERATION_NOT_PERMITTED = "Cannot make this operation at current state"

module.exports = {
    CANNOT_MOVE_DURING_STORM,
    CANNOT_MOVE_OUT_OF_BOUND,
    OPERATION_NOT_PERMITTED
}